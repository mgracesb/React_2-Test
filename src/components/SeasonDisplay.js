import React from "react";

const SeasonDisplay = (props) => {
  return (
    <div>
      <h1>Latitude:</h1>
      {props.season}
    </div>
  );
};

//detect physical location of user

//detect current month

//change text message and styling based on the location and month of user

export default SeasonDisplay;
